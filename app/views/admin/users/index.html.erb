<% provide :title, t(".title") %>
<div class="col-lg-12 grid-margin stretch-card">
  <div class="card">
    <div class="card-body">
      <h4 class="card-title"><%= t(".title") %></h4>
      <div class="d-flex justify-content-between flex-row">
        <%== pagy_bootstrap_nav(@pagy) %>
        <%== pagy_items_selector_js(@pagy) %>
      </div>
      <div class="table-responsive">
        <table class="table sortable-table" id="users-table" data-toggle="bootstrap-table">
          <thead>
            <tr>
              <th class="sortStyle"><%= t ".profile" %></th>
              <th class="sortStyle"><%= t ".full_name" %></th>
              <th class="sortStyle"><%= t ".phone" %></th>
              <th class="sortStyle"><%= t ".dob" %></th>
              <th class="sortStyle"><%= t ".join_from" %></th>
              <th class="sortStyle"><%= t ".status" %></th>
            </tr>
          </thead>
          <tbody>
            <%= render partial: "user", collection: @users %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
